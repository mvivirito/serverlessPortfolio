version: 0.1

phases:
    install:
        runtime-versions:
            node: 8.0
    build:
        commands:
            - echo nothing to do yet
artifacts:
    files:
        - index.html
        - styles/*
        - images/*
        - favicon.ico
